name: Linux Release

on:
  push:
    tags:
      - '*'
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      - matrix.os: ubuntu-latest
        matrix.target: x86_64-unknown-linux-gnu
        matrix.name: fluyendo-ubuntu-glibc
      - matrix.os: ubuntu-latest
        matrix.target: x86_64-unknown-linux-musl
        matrix.name: fluyendo-ubuntu-musl

    steps:
    - uses: actions/checkout@v2
    - name: Install sysdeps
      run: |
        sudo apt-get update
        sudo apt-get -y install libxkbcommon-dev
    - uses: actions-rs/toolchain@v1
      with:
        components: rustfmt, clippy
    - name: Build
      run: |
        cargo build --release --target-dir ${{ matrix.target }} --target ${{ matrix.target }}
    - name: Upload binary
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: ${{ matrix.target }}/release/fluyendo
        asset_name: ${{ matrix.name }}
        tag: ${{ github.ref }}
